# Notas

Este es mi primer programa node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --numero   Número que se requiere multiplicar          [number] [required]
  -l, --listar   Muestra los valores que se copiaran al archivo
                                           [boolean] [required] [default: false]
  -h, --hasta    Hasta que numero se efectuara la multiplicación
                                                             [number] [required]
```